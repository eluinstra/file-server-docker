# DEMO / DEVELOPMENT VALUES ONLY — basic-auth credentials for the local docker-compose
# example in this directory. NOT for production: the "admin" password is weak and this
# file is checked in. The file is the format expected by the file-server basic-auth realm
# (PBKDF2, salted) so the server starts non-interactively. For any non-local run, generate
# a fresh strong credential (admin prompt on first start) and never commit it.
admin AAM0UJl8uSSmsiV34Hx7BnvRgyD3/DW/aWOr5zp4qFNVezo9yluqztg0UTtyvxNSx6uv4w==
